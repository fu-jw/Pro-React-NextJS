import HeroSection from "./HeroSection";

const AuthScreen = () => {
  return (
    // 弹性布局，垂直方向
    <div className="flex flex-col">
      <HeroSection />
      
    </div>
  );
};

export default AuthScreen;
